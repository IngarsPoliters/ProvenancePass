{
  "name": "@passport/manifest-store",
  "version": "0.1.0",
  "description": "Minimal API for receipts by SHA-256",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write src",
    "clean": "rimraf dist",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "fastify": "^4.25.0",
    "@fastify/cors": "^9.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "tsx": "^4.7.0"
  },
  "keywords": [
    "api",
    "manifest",
    "storage",
    "provenance"
  ],
  "license": "Apache-2.0"
}