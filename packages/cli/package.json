{
  "name": "@passport/cli",
  "version": "0.1.0",
  "description": "TypeScript CLI & Node SDK for Provenance Passport",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "passport": "dist/cli.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write src",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0"
  },
  "keywords": [
    "cli",
    "provenance",
    "verification"
  ],
  "license": "Apache-2.0"
}